{
  "private": true,
  "scripts": {
    "test-all": "lerna run test-all --concurrency 2",
    "build": "lerna run build --concurrency 2",
    "bootstrap": "lerna bootstrap"
  },
  "dependencies": {
    "lerna": "^3.10.7"
  }
}
